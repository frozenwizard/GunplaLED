{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GundamLEDConfig",
  "type": "object",
  "required": ["name", "leds", "lightshow"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The model name of the Gundam unit."
    },
    "leds": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "pin", "color"],
        "properties": {
          "name": { "type": "string" },
          "pin": { "type": "integer", "minimum": 0, "maximum": 21 },
          "color": { "type": "string" },
          "disabled": { "type": "boolean", "default": false }
        }
      }
    },
    "lightshow": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "path", "method"],
        "properties": {
          "name": { "type": "string" },
          "path": { "type": "string" },
          "method": { "type": "string" }
        }
      }
    }
  }
}